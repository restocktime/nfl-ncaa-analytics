services:
  - type: web
    name: nfl-analytics-pro
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node nfl-server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000